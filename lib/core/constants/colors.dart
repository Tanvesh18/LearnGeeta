import 'package:flutter/material.dart';

class AppColors {
  static const saffron = Color(0xFFFF9933);
  static const gold = Color(0xFFE6B566);
  static const cream = Color(0xFFFFF8E7);
  static const deepBrown = Color(0xFF4E342E);
  static const softGrey = Color(0xFFF5F5F5);

  // New colors to match home_screen: success green like +XP, error soft red
  static const success = Colors.green;
  static const error = Color(0xFFE57373);

  // Gradients to match home feel (cream-orange light)
  static const gradientStart = Color(0xFFFFF8F0);
  static const gradientEnd = Color(0xFFFFEBD6);

  // UI consistency
  static const cardColor = Colors.white;
  static const shadowColor = Colors.black26;
}
